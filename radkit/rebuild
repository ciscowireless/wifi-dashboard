docker stop radkit2influx
docker rm radkit2influx
docker rmi radkit2influx
docker build -t radkit2influx .
docker run -d --network host --restart always --name radkit2influx radkit2influx
docker ps
 
